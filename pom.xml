<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>kmeans</groupId>
    <artifactId>kmeansAndEM</artifactId>
    <version>1</version>

    <properties>
        <jamaVersion>1.0.3</jamaVersion>
    </properties>

    <dependencies>
        <dependency>
            <groupId>gov.nist.math</groupId>
            <artifactId>jama</artifactId>
            <version>${jamaVersion}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>